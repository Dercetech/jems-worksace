import { ImageAsset } from "../models";

const folder = "backgrounds";

export const MAIN_MENU: ImageAsset = {
  key: "mainMenu",
  url: `${folder}/main-menu.png`,
};

export const PRELOAD_LIST: ImageAsset[] = [];
